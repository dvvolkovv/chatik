# 🚂 Деплой на Railway - Пошаговая инструкция

## Что такое Railway?

Railway - это современная платформа для деплоя приложений. Предоставляет:
- PostgreSQL, Redis, MySQL из коробки
- Автоматический CI/CD из GitHub
- Бесплатный tier ($5 credit каждый месяц)
- HTTPS сертификаты автоматически
- Простое управление через CLI или веб-интерфейс

---

## 🎯 Что нужно подготовить на Railway

### Сервисы для создания:

1. **PostgreSQL** - основная база данных
2. **Redis** - кэш и сессии
3. **Backend** - FastAPI приложение

---

## 📋 Пошаговая инструкция

### Шаг 1: Создайте аккаунт на Railway

1. Перейдите на https://railway.app/
2. Зарегистрируйтесь через GitHub
3. Подтвердите email

### Шаг 2: Создайте новый проект

1. Нажмите "New Project"
2. Выберите "Empty Project"
3. Назовите проект: `ai-chat-platform`

### Шаг 3: Добавьте PostgreSQL

1. Нажмите "+ New" в проекте
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст БД
4. Скопируйте `DATABASE_URL` из Variables

### Шаг 4: Добавьте Redis

1. Нажмите "+ New"
2. Выберите "Database" → "Redis"
3. Railway автоматически создаст Redis
4. Скопируйте `REDIS_URL` из Variables

### Шаг 5: Подготовьте код для GitHub

```bash
cd ai-chat-backend

# Создайте .gitignore
cat > .gitignore << EOF
__pycache__/
*.py[cod]
*$py.class
.env
.venv
venv/
*.log
uploads/
.pytest_cache/
.coverage
htmlcov/
EOF

# Инициализируйте git
git init
git add .
git commit -m "Initial commit: FastAPI backend"

# Создайте репозиторий на GitHub и загрузите
git remote add origin https://github.com/YOUR_USERNAME/ai-chat-backend.git
git branch -M main
git push -u origin main
```

### Шаг 6: Деплой Backend на Railway

#### Вариант A: Через GitHub (рекомендуется)

1. В проекте Railway нажмите "+ New"
2. Выберите "GitHub Repo"
3. Выберите ваш репозиторий `ai-chat-backend`
4. Railway автоматически обнаружит Dockerfile
5. Нажмите "Deploy"

#### Вариант B: Через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Свяжите с проектом
railway link

# Деплой
railway up
```

### Шаг 7: Настройте переменные окружения

В Railway UI:

1. Откройте ваш Backend сервис
2. Перейдите в "Variables"
3. Добавьте переменные:

```bash
# Секретные ключи
SECRET_KEY=<сгенерируйте секретный ключ>
JWT_SECRET_KEY=<сгенерируйте JWT ключ>

# База данных (автоматически из PostgreSQL сервиса)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (автоматически из Redis сервиса)
REDIS_URL=${{Redis.REDIS_URL}}

# LLM API ключи
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AIza...

# CORS
CORS_ORIGINS=https://your-frontend-url.com,http://localhost:8888

# Опционально
DEBUG=False
APP_ENV=production
```

**Генерация ключей:**
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Шаг 8: Примените миграции БД

#### Вариант A: Через Railway CLI

```bash
# Подключитесь к проекту
railway link

# Запустите миграции
railway run alembic upgrade head
```

#### Вариант B: Добавьте в Dockerfile

Измените `Dockerfile` для автоматических миграций:

```dockerfile
# В конце файла, перед CMD
RUN alembic upgrade head

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### Вариант C: Через Railway Shell

1. В Railway UI откройте Backend сервис
2. Перейдите во вкладку "Settings"
3. Нажмите "Open Shell"
4. Выполните:
   ```bash
   alembic upgrade head
   ```

### Шаг 9: Проверьте деплой

```bash
# Railway предоставит вам URL, например:
# https://ai-chat-backend-production.up.railway.app

# Проверьте health check
curl https://your-app.railway.app/health

# Откройте документацию
https://your-app.railway.app/docs
```

### Шаг 10: Настройте домен (опционально)

1. В Railway UI откройте Backend сервис
2. Перейдите в "Settings" → "Domains"
3. Нажмите "Generate Domain" или добавьте свой кастомный домен
4. Railway автоматически настроит HTTPS

---

## 🔧 Настройка после деплоя

### 1. Проверьте логи

```bash
railway logs
```

Или в Railway UI: вкладка "Deployments" → View Logs

### 2. Мониторинг

Railway предоставляет:
- CPU usage
- Memory usage
- Network traffic
- Build logs
- Deploy logs

### 3. Масштабирование

В Settings можно настроить:
- Replicas (количество инстансов)
- Resources (CPU, RAM)
- Autoscaling

---

## 💰 Стоимость на Railway

### Free Tier
- $5 кредитов каждый месяц
- Достаточно для разработки и тестирования
- ~500 часов работы маленьких сервисов

### Paid Plans
- Pay as you go
- ~$5-20/месяц для небольшого проекта
- Зависит от использования ресурсов

### Примерная стоимость вашего проекта:
```
Backend (FastAPI):    $5-10/месяц
PostgreSQL:           $5/месяц
Redis:                $2/месяц
---
Итого:                ~$12-17/месяц
```

---

## 🔄 Автоматический деплой

После настройки GitHub интеграции:

```bash
# Любой push в main автоматически деплоится
git add .
git commit -m "Update feature"
git push origin main

# Railway автоматически:
# 1. Обнаружит изменения
# 2. Соберёт новый Docker образ
# 3. Задеплоит новую версию
# 4. Переключит трафик без downtime
```

---

## 🐛 Troubleshooting

### Проблема: Build fails

**Решение:**
- Проверьте `requirements.txt` на опечатки
- Убедитесь, что Dockerfile корректный
- Посмотрите build logs в Railway

### Проблема: Database connection error

**Решение:**
- Убедитесь, что `DATABASE_URL` установлен
- Проверьте формат: `postgresql+asyncpg://user:pass@host:port/db`
- Используйте `${{Postgres.DATABASE_URL}}`

### Проблема: Migrations не применяются

**Решение:**
```bash
# Подключитесь и выполните вручную
railway run alembic upgrade head
```

### Проблема: 502 Bad Gateway

**Решение:**
- Проверьте, что приложение слушает `0.0.0.0`, а не `127.0.0.1`
- Используйте правильный PORT: `--port $PORT`
- Проверьте logs

---

## 📚 Полезные команды Railway

```bash
# Логи
railway logs

# Переменные
railway variables

# Запустить команду
railway run <command>

# Shell
railway shell

# Статус
railway status

# Открыть в браузере
railway open
```

---

## ✅ Чеклист деплоя

- [ ] PostgreSQL создан на Railway
- [ ] Redis создан на Railway
- [ ] Код загружен на GitHub
- [ ] Backend сервис создан из GitHub репо
- [ ] Переменные окружения настроены
- [ ] Миграции применены
- [ ] Health check проходит
- [ ] API docs доступны
- [ ] CORS настроен для фронтенда
- [ ] Домен настроен (опционально)

---

## 🎉 Готово!

После выполнения всех шагов:

1. ✅ Backend работает на Railway
2. ✅ PostgreSQL и Redis подключены
3. ✅ Автоматический деплой настроен
4. ✅ HTTPS работает
5. ✅ API доступен по URL

**Ваш backend URL:**
```
https://your-project.up.railway.app
```

**API документация:**
```
https://your-project.up.railway.app/docs
```

---

## 🔗 Следующие шаги

1. Подключите фронтенд к новому API
2. Получите LLM API ключи (OpenAI, Anthropic)
3. Настройте платежную систему (ЮKassa)
4. Добавьте мониторинг (Sentry)

---

**Успешного деплоя! 🚀**

*Создано для AI Chat Platform*
